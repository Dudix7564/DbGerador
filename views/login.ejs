<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #d379cd; /* Fundo rosa suave */
            color: #000; /* Texto preto */
            font-family: 'Arial', sans-serif;
        }

        .container {
            background-color: #9d058c; /* Rosa vibrante */
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0px 15px 40px rgba(255, 105, 180, 0.7);
            max-width: 500px;
        }

        h1 {
            color: #fff;
            font-size: 3rem;
            font-weight: 600;
            margin-bottom: 20px;
        }

        .form-label {
            color: #fff;
        }

        .form-control {
            background-color: #fff; /* Campos de entrada brancos */
            color:#9d058c; /* Texto rosa */
            border: 2px solid #e309c9; /* Borda rosa */
            transition: all 0.3s ease-in-out;
        }

        .form-control:focus {
            border-color: #9d058c;
            box-shadow: 0 0 10px rgba(125, 15, 100, 0.7); /* Efeito ao focar */
        }

        .btn-custom {
            background-color: #000; /* Botão preto */
            color: #fff; /* Texto branco */
            border: 2px solid #9d058c;
            padding: 15px 25px;
            font-size: 1.1rem;
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
        }

        .btn-custom:hover {
            background-color: #9d058c;
            color: #fff;
            border-color: #fff;
            transform: scale(1.1);
        }

        .d-flex {
            justify-content: center;
            gap: 20px;
        }

        .text-custom-link {
            color: #150513; /* Rosa suave para links */
        }

        .text-custom-link:hover {
            text-decoration: none;
            color: #020002;
        }
    </style>
</head>
<body class="d-flex justify-content-center align-items-center vh-100">

    <div class="container text-center">
        <h1 class="mb-4">Login</h1>
        <form action="/login" method="POST" class="row g-3">
            <div class="col-md-12">
                <label for="inputMatricula" class="form-label">Matrícula</label>
                <input type="number" class="form-control" id="inputMatricula" name="Matrícula" required>
            </div>
            <div class="col-md-12">
                <label for="inputNome" class="form-label">Nome</label>
                <input type="text" class="form-control" id="inputNome" name="NomeUsuário" required>
            </div>
            <button type="submit" class="btn btn-custom btn-lg">Entrar</button>
        </form>
        <p class="mt-3">Ainda não possui uma conta? <a href="/cadastro" class="text-custom-link">Cadastre-se</a></p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
