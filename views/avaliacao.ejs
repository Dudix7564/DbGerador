<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Avaliação</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fce4ec; /* Rosa suave de fundo */
            color: #000; /* Texto preto */
        }

        .container {
            background-color: #fff; /* Branco no container */
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #d81b60; /* Rosa suave para o título */
            margin-bottom: 20px;
        }

        .alert-info {
            background-color: #f8bbd0; /* Rosa claro */
            color: #000;
            font-weight: normal;
        }

        .form-label {
            color: #d81b60; /* Rosa mais suave para o texto dos campos */
        }

        .form-control {
            border: 1px solid #d81b60;
            border-radius: 5px;
            color: #000;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #d81b60;
            box-shadow: 0 0 5px rgba(216, 27, 96, 0.5);
        }

        .form-select {
            border: 1px solid #d81b60;
            color: #000;
        }

        .btn-light {
            background-color: #d81b60; /* Botão rosa */
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1rem;
            transition: background-color 0.3s ease;
        }

        .btn-light:hover {
            background-color: #f50057;
        }
    </style>
</head>

<body>
    <div class="container mt-5 mb-5">
        <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Gerenciador de Filmes e Séries</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="/usuario/<%= matricula %>">Usuário</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/gerador/<%= matricula %>">Gerador</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="#">Avaliação</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <h1 class="text-center">Avaliação de Filmes ou Séries</h1>

        <div class="alert alert-info text-center">
            Sua matrícula: <strong><%= matricula %></strong>
        </div>

        <form action="/avaliacao/<%= matricula %>" method="POST" class="row g-3">
            <input type="hidden" name="matricula" value="<%= matricula %>"> <!-- Matrícula oculta no form -->

            <div class="col-md-12">
                <label for="inputIdMidia" class="form-label">Código do Filme ou Série</label>
                <input type="text" class="form-control" id="inputIdMidia" name="CodMídia" required>
            </div>

            <div class="col-md-12">
                <label for="inputNota" class="form-label">Nota</label>
                <select id="inputNota" class="form-select" name="NotaAvaliação" required>
                    <option selected disabled>Escolha aqui...</option>
                    <option value="1">1 - Péssimo</option>
                    <option value="2">2 - Ruim</option>
                    <option value="3">3 - Regular</option>
                    <option value="4">4 - Bom</option>
                    <option value="5">5 - Excelente</option>
                </select>
            </div>

            <button type="submit" class="btn btn-light">Avaliar</button>
        </form>
    </div>

    <script>
        // Captura o parâmetro "filme" da URL e preenche automaticamente o campo "Código do Filme"
        const urlParams = new URLSearchParams(window.location.search);
        const filme = urlParams.get('filme');
        if (filme) {
            document.getElementById('inputIdMidia').value = filme;
        }
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmX5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>
</html>
